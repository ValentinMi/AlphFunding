TxScript Contribute(
    pool: Pool,
    amount: U256
) {
    pool.contribute{callerAddress!() -> ALPH: amount + mapEntryDeposit!()}(amount)
}

TxScript Withdraw(
    pool: Pool
) {
    pool.withdraw()
}

TxScript Refund(pool: Pool) {
    pool.refund()
}