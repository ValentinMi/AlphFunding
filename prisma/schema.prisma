generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./pools.db"
}

model Pool {
  id              Int    @id @default(autoincrement())
  contractAddress String
}
